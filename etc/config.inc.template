<?php
/**
 * /opt/nac/etc/config.inc.template
 *
 * Long description for file:
 * ...
 *
 * PHP version 5
 *
 * LICENSE: This program is free software; you can redistribute it and/or
 * modify it under the terms of the GNU General Public License as published
 * by the Free Software Foundation.
 *
 * @package			FreeNAC
 * @author			Sean Boran (FreeNAC Core Team)
 * @copyright		2006 FreeNAC
 * @license			http://www.gnu.org/copyleft/gpl.html   GNU Public License Version 2
 * @version			CVS: $Id:$
 * @link				http://www.freenac.net
 *
 */

## Enterprise config options
#require_once "/opt/nac/enterprise/config_en.inc";


## MySQL DB settings for all scripts
  $dbhost="localhost";
  $dbname="inventory";
  $dbuser="inventwrite";
  $dbpass="PASSWORD1";              # keep this secret!

## Debug 
  $debug_to_syslog=true;          # Daemons: Debug also to syslog?
  $logit_to_stdout=false;         # Debugging to the console?

## vmps_external script:  
  $use_port_default_vlan=true;     # should a default port per default be checked? If not only
                                   # the global $default_vlan will be checked.
  $detect_hubs=true;               # 'Intelligent' algorithm to resolve conflicts of several devices
                                   # on one port, i.e. an un-managed switch or hub
  $default_vlan=0;                 # 0=Default deny, otherwise unknown devices are put on this vlan

## vmps_lastseen script:  
  $default_user_unknown='KEINE';   # Unknown devices are assigned to this User ID
  $lastseen_dryrun=false;           # Test, don't update DB
  $set_vlan_for_unknowns="vlan=0,"; # Set to "vlan=0," deny unknowns. The number corresponding to 
                                    # an entry in the vlan table.
                                    # (don't forget the comma :-)
  $lastseen_patch_lookup=false;     # Lookup Patchcable details in Alerts
                                    # set false by default since most people don't use the patch table

## Binding with FreeRadius
  $vmps_servers = "localhost";	    #Several VMPS servers to query. If one fails, go and query to the next one. List has to be delimited by commas
				    #Example: $vmps_servers = "vmps1,vmps2,vmps3";  


# enterprise legacy issues, leave false
  $lastseen_infnet=false;          
  $lastseen_infnet_notify=false;
  $check_infnet_mac="";
  $sms_infnet_mac="";


  $mail_user="root@DOMAIN.ch";      # mail to root? Set your domain
  #$maildomain="0";                 # set to 0 to disable.
  $maildomain="DOMAIN.ch";          # 

  $vmps_monitor_restart=FALSE;
  $os_hostname="vmps1";           # for alerting



## report_old_users script:           
  $report_old_users_days_from=3;
  $report_old_users_days_back=10;   # Check for Direx users not updated 
                                    # between -3 and this number of days

## vmps_check & vmps_monitor script:
  $vmpsd_is_local="1";              # =1 For Version2 or later
  $vmps_lastseen_is_local="1";      # =1 
  $server1="";                      # 
  $server2="";
  $check_port_restart=true;         # Restart switch port if flagged

## vmps_user script:
  $vmps_user_ska=array('XXX*');    # Organisations be synced from Direx
  $vmps_user_direx="direx.test.com";
  $vmps_user_direx_path="/my/url/GetXML.aspx";
  
## purge_unknowns :
  $unknown_purge=30;               # Delete unknown systems older than XX days


## SNMP communities
  $snmp_ro="PASSWORD2";            # maybe public	
  $snmp_rw="PASSWORD3";            # very private

## snmp_import
  $snmp_dryrun=TRUE;               # Do not update MySQL after SNMP queries

## restart_port
  $snmpwalk="/usr/bin/snmpwalk -v 1 -c $snmp_ro ";  # SNMP Read community
  $snmpset ="/usr/bin/snmpset  -v 1 -c $snmp_rw";   # SNMP Write community

## router_mac_ip
  $core_routers="192.168.245.3 192.168.245.6";
  $router_mac_ip_ignore_ip="/^(127\.0\.0|192\.168\./";
  $router_mac_ip_ignore_mac="/^(00d0\.0064\.d000)/";
  $router_snmpwalk="/usr/bin/snmpwalk -v 1 -c $snmp_ro ";    # SNMP Read
  $router_mac_ip_update_from_dns=false;     # Auto-update system names from DNS for 'unknowns'?


## monitor_allows.sh
#  $messagecount=50;

## flap_detect
  $flap_limit=40;
  

## LDAP coniguration settings (Active Directory)
## ad_user_sync
  $ad_server = 'dc.test.xx';		# Domain controller
  $ad_user = 'cn=administrator,cn=users,dc=test,dc=xx';	# AD user with read privileges on AD
  $ad_password = '';			# AD users's password
  $ad_base_user_dn = array('cn=users,dc=test,dc=xx');	# List with base DNs under which user information is stored


?>
